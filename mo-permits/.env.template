# Copy this to .env and fill in your values
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=stlmeetings@veiledprofits.com
SMTP_PASS=your-password-here

# Optional: Cloudflare Worker proxy URL if dnr.mo.gov is blocked from your VPS
# Same pattern as stl-meetings — only set this if you get connection errors
# CF_PROXY_URL=https://your-worker.workers.dev/?url=
