# Copy to .env and fill in real values
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=permits@veiledprofits.com
SMTP_PASS=REPLACE_ME
BASE_URL=https://permits.veiledprofits.com

# Cloudflare Worker proxy (reused from stl-meetings)
PROXY_BASE=https://stl-proxy.dan-f8a.workers.dev/?url=

# When to send the daily digest (server local hour, 24h)
DIGEST_HOUR=7

# How often to pull the city feed
SYNC_INTERVAL_HOURS=6
